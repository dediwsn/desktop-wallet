import React from "react";

import { Send } from "./Send";

export default {
	title: "Transaction / Components / Send Transaction / Step 1",
};

export const Step1 = () => (
	<div>
		<Send></Send>
	</div>
);
